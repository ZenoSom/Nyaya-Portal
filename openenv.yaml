spec_version: 1
name: nyaya_portal
description: Nyaya Portal - Court Case Lookup System.
type: space

entrypoint:
  module: server.app
  callable: app

server:
  host: 0.0.0.0
  port: 7860

endpoints:
  health: /health
  tasks: /tasks
  reset: /reset
  step: /step
  state: /state
  grader: /grader
  baseline: /baseline

